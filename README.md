# Alba Museum — Starter

Estructura base para la web tipo **museo/galería** (React + Vite + Tailwind + Framer Motion).  
Incluye solo arquetipo de carpetas y archivos con **TODOs**, sin el código del prototipo.

## Pasos rápidos
1. Instalar dependencias:
   ```bash
   npm i
   ```
2. Ejecutar en desarrollo:
   ```bash
   npm run dev
   ```


> Sugerencia: crea carpetas `src/assets/` para tus imágenes y `src/data/` para metadatos de colecciones.
